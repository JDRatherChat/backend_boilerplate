# Development via Docker Compose
# Copy this file to environments/dev_docker.env and adjust as needed.
ENV=dev_docker

DEBUG=1
ALLOWED_HOSTS=*

# Postgres (compose service name is `db`)
DATABASE_URL=postgres://app:app@db:5432/backend_boilerplate
