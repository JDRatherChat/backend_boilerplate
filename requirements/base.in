"""Base (prod-safe) requirements.

Notes:
- This template standardises on Django 4.2 LTS.
- Keep this file *lean*: only runtime deps.
"""

# ---- Core Django ----
Django>=4.2,<4.3

# ---- Environment variables ----
django-environ

# ---- Database (local dev & CI) ----
# psycopg v3 with bundled libpq; good for local dev as well.
psycopg[binary]

# ---- REST API ----
djangorestframework
djangorestframework-simplejwt
drf-spectacular
djoser

# ---- Logging & Observability ----
sentry-sdk[django]
structlog
django-structlog
python-json-logger

# ---- CORS for Vue frontend during dev ----
django-cors-headers

# ---- Developer utilities ----
django-extensions

# ---- Async / Background work ----
celery
redis

# ---- Object storage (S3) ----
django-storages[boto3]
