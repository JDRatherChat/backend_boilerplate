# ---- Core Django ----
Django>=4.2,<5.0

# ---- Environment variables ----
django-environ

# ---- Database (local dev & CI) ----
# psycopg v3 with bundled libpq; perfect for Windows/macOS dev boxes
psycopg[binary]

# ---- REST API ----
djangorestframework
djangorestframework-simplejwt
drf-spectacular
# Optional but handy if you want Swagger UI assets without internet
# drf-spectacular-sidecar

# ---- CORS for Vue frontend during dev ----
django-cors-headers

# ---- Developer utilities ----
django-extensions
